{
  "name": "UserAccount",
  "type": "object",
  "properties": {
    "username": {
      "type": "string",
      "description": "Unique username"
    },
    "email": {
      "type": "string",
      "description": "Email address (optional)"
    },
    "password_hash": {
      "type": "string",
      "description": "Hashed password"
    },
    "login_method": {
      "type": "string",
      "enum": [
        "username",
        "email",
        "both"
      ],
      "default": "both",
      "description": "How the user can log in"
    },
    "last_login": {
      "type": "string",
      "format": "date-time",
      "description": "Last login timestamp"
    },
    "device_type": {
      "type": "string",
      "description": "mobile or desktop"
    },
    "os": {
      "type": "string",
      "description": "Operating system detected at registration"
    },
    "browser": {
      "type": "string",
      "description": "Browser detected at registration"
    },
    "role": {
      "type": "string",
      "enum": [
        "user",
        "admin"
      ],
      "default": "user",
      "description": "User role"
    }
  },
  "required": [
    "username",
    "password_hash"
  ],
  "rls": {
    "create": {
      "$or": [
        {},
        {
          "created_by": "{{user.email}}"
        }
      ]
    }
  }
}